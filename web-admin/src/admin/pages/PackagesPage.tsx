import { SectionPlaceholderPage } from "./SectionPlaceholderPage";

export function PackagesPage() {
  return (
    <SectionPlaceholderPage
      title="Packages"
      description={
        "Package creation and billing lifecycle tools will be wired after API client foundation."
      }
    />
  );
}
