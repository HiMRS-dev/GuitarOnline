import { SectionPlaceholderPage } from "./SectionPlaceholderPage";

export function StudentsPage() {
  return (
    <SectionPlaceholderPage
      title="Students"
      description={
        "Student roster and package linkage views are reserved for the next implementation steps."
      }
    />
  );
}
